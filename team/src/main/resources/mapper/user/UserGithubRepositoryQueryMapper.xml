<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sansam.team.user.query.mapper.UserGithubRepositoryMapper">

    <!-- 깃허브 레포지토리 전체 조회 -->
    <select id="findAllGithubRepo" resultType="UserGithubRepositoryQueryDTO">
        SELECT
            user_repository_url AS UserRepositoryUrl,
            user_repository_name AS UserRepositoryName,
            user_repository_type AS userRepositoryType
          FROM tbl_user_github_repository
    </select>

</mapper>